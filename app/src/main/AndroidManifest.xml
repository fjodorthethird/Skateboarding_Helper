<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="cz.zelgadiss.myapplication">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:remove="android:maxSdkVersion" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:remove="android:maxSdkVersion" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android:permission:INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".GameActivity"></activity>
        <activity
            android:name=".ProgressActivity"
            android:label="Progress Activity"></activity>
        <activity
            android:name=".LoginActivity"
            android:label="Login Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegistrationActivity"
            android:label="Registration Activity" />
        <activity
            android:name=".MainActivity"
            android:label="Main Activity" />
        <activity
            android:name=".Menu_About_Activity"
            android:label="About Activity" />
        <activity
            android:name=".Menu_Settings_Activity"
            android:label="Settings Activity" />
        <activity
            android:name=".EncyActivity"
            android:label="Encyclopedia Activity" />
        <activity
            android:name=".ProfileActivity"
            android:label="Profile Activity" />
    </application>

</manifest>